# Admin Dashboard (Next.js + Material UI)

This folder will contain the web-based administration dashboard implemented with Next.js and Material UI.

Key tasks:

- Implement authentication via backend
- Role-based UI and access controls
- CRUD pages for major entities
- Analytics and audit logs
